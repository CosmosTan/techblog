# Task 3: Enable local search

## Spec
In `docs/.vitepress/config.ts`, add local search configuration to replace the unconfigured Algolia.

```ts
// Add to themeConfig
search: {
  provider: 'local'
}
```

## Action
Edit `docs/.vitepress/config.ts` to add the search config in themeConfig section.
